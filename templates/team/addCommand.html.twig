{% extends 'base.html.twig' %}
{% block title %}Add command{{ parent() }}{% endblock %}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
{% block body %}
    {{ include ('navBar/navBarOrganizer.html.twig') }}
    <div class="container container-index">
        <div class="row justify-content-center">
            <div class="col-lg-10">
                <div class="card mt-4 card-shadow">
                    <img class="card-img-top img-fluid" src="{{ asset('/images/back.jpg') }}" alt="">
                    <div class="card-body">
                        <h3 class="card-title">Komandos pridėjimas</h3>
                        <p class="card-text">
                            {{ form_start(form) }}
                            {{ form_widget(form) }}
                            {{ form_end(form) }}
                        </p>
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}